#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

//~ #include "symbols.hpp"
#include "scanner.cpp"

int main()
{

	//~ string str = "!==";

		//~ string * res = std::find(std::begin(operators), std::end(operators), str);
		//~ if( res != std::end(operators) ){
			//~ cout << "ok" << endl;
		//~ }
	
	

  return 0;
}
